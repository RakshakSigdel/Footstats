PORT=
DATABASE_URL=
JWT_SECRET=
FRONTEND_URL=
EMAIL_USER=
EMAIL_PASSWORD=
# Must match frontend VITE_GOOGLE_CLIENT_ID. Do not wrap in quotes.
GOOGLE_CLIENT_ID=
# Not used by current ID-token flow. Keep server-side only for future auth-code flow.
GOOGLE_CLIENT_SECRET=