import 'package:flutter/material.dart';

class ProfileClubsScreen extends StatelessWidget {
  const ProfileClubsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Profile Clubs Screen"),);
  }
}
