import 'package:flutter/material.dart';

class ProfileMatchesScreen extends StatelessWidget {
  const ProfileMatchesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Profile Matches Screen"),);
  }
}
